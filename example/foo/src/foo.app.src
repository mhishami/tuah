{application, foo, [
    {description, ""},
    {vsn, "0.1.0"},
    {modules, []},
    {registered, [foo_sup]},
    {applications, [
                kernel,
                stdlib,
                crypto,
                mnesia,
                tuah,
                sync,
                cowboy,
                erlydtl,
                jsx,
                lager,
                cowlib,
                ranch,
                bson,
                pbkdf2,
                mongodb
    ]},
    {mod, {foo_app, []}},
    {env, [
        {http, [{port, 8080}]},
        {pools, [
            {foo, [
            {size, 10},
            {max_overflow, 30}
        ], [
            {database, <<"foo">>},
            {w_mode, safe}
            ]}
        ]}
    ]}
]}.
